Explicacion de la presentacion.
Revisar la presentacion para para tomar datos.

vagran up : para up: para encencer la maquina
vagran ssh: acceder a la maquina 
sudo hostnamectl set-hostname webserver : cambia el nombre a la maquina en este caso le colocamos
webserver

host file: simula un nombre a una ip como un Dns
sudo nano /etc/host para edidar el archivo y cambiar el archivo bookworn
ctrl + o :  para guardar y enter para confimar que vamos a guardar el al archivo.
ctrl + x : salir de la ventana de editar
luego le damos exit, y volvemos a reiniciar la maquina con  vagran up

sudo apt-get update: actualizar la lista de paquetes elegibles

instalar paquetes como vim, curl, apache2, My
sudo apt-get install vim
tabulador: predice la palabra.
sudo apt-get install vim vim-nox curl apache2 mariadb-server mariadb-client php8.2 php8.2-curl
php8.2-bcmath php8.2-mysql php8.2-mcrypt php8.2-xml php8.2-zip php8.2-mbstring : instalamos todos los paquetes.
ping 192.168.56.10 hacer ping desde la maquina anfitriona a la virtual.

en el cmd como administrador ejecutamos 
cd drivers\etc
luego nodepad host y esto nos abre un archivo y agregamos esta ruta=  192.168.56.10	brian.isw.xyz
luego le hacemos ping a esa ruta desde la maquina anfitriona.

sudo a2enmod vhost_alias rewrite ssl : 
vemos 	que existen dos carpetas 
cargamos una imagen jpg y la vemos desde la maquina virtual.
cremos una careta sities con mkdir sities